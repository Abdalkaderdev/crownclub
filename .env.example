# Public, view-only Google Sheet holding the live menu.
# Columns: Category | Name | Description | Price | Bottle | Available | Tags
# Server-only on purpose: a NEXT_PUBLIC_ prefix would inline the id into the
# JavaScript bundle every visitor downloads.
SHEET_ID=
SHEET_GID=0
